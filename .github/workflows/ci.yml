name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test-frontend:
    name: Frontend tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci
      - run: npm test

  test-rust:
    name: Rust tests & lint
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - name: Install macOS system deps
        run: |
          brew install pkg-config

      - name: Clippy
        working-directory: src-tauri
        # Advisory, not blocking: CI installs the latest stable clippy, so `-D warnings`
        # turned every newly-added upstream lint into a release-blocking failure even
        # when nothing in our code changed. Warnings still print; only genuine clippy
        # *errors* (deny-by-default) fail the job. Pin a toolchain if strictness is wanted.
        run: cargo clippy --all-targets

      - name: Tests
        working-directory: src-tauri
        run: cargo test

  build:
    name: Build (release, no bundle)
    runs-on: macos-latest
    needs: [test-frontend, test-rust]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - run: npm ci
      - run: npm run tauri:build -- --no-bundle
